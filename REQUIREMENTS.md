# Level 3: 컴포넌트 분리 + 목록 분리 + 정렬 + 수정

## 목표

TodoList의 CRUD 기능을 구현하되, 컴포넌트를 분리하고 할일/완료 목록을 나누고, 정렬 기능과 할일 제목 수정 기능까지 구현합니다.

## 사용 개념

- `useState`
- 컴포넌트 분리
- `props`

## 데이터 구조

```js
{
  id: 1,
  title: "할일",
  completed: false,
  createdAt: "2026-03-18T12:00:00.000Z"
}
```

## 요구사항

### 1. 할일 추가 (Create)

- 텍스트 입력창과 "추가" 버튼이 있어야 합니다
- 입력창에 할일을 입력하고 "추가" 버튼을 클릭하면 새로운 할일이 목록에 추가됩니다
- 추가 후 입력창은 비워져야 합니다
- 빈 문자열은 추가할 수 없어야 합니다

### 2. 할일 목록 / 완료 목록 분리 표시 (Read)

- **할일 목록**: `completed`가 `false`인 항목만 표시
- **완료 목록**: `completed`가 `true`인 항목만 표시
- 각 섹션에 제목(`h2`)이 있어야 합니다
- 각 할일에는 제목과 생성 시간이 보여야 합니다

### 3. 완료 처리 / 취소 (Update)

- 할일 목록의 항목에는 "완료" 버튼이 있어야 합니다
- "완료" 버튼을 누르면 해당 항목이 완료 목록으로 이동합니다
- 완료 목록의 항목에는 "취소" 버튼이 있어야 합니다
- "취소" 버튼을 누르면 해당 항목이 다시 할일 목록으로 이동합니다

### 4. 할일 삭제 (Delete)

- 각 할일에 "삭제" 버튼이 있어야 합니다
- "삭제" 버튼을 누르면 해당 할일이 목록에서 제거됩니다

### 5. 할일 제목 수정 (Update - 추가 기능)

- 각 할일에 "수정" 버튼이 있어야 합니다
- "수정" 버튼을 누르면 해당 할일의 제목이 입력창으로 바뀝니다
- 입력창에는 기존 제목이 미리 채워져 있어야 합니다
- "저장" 버튼을 누르면 수정된 제목이 저장됩니다
- 빈 문자열로는 수정할 수 없어야 합니다

### 6. 정렬 기능

- 정렬 버튼이 있어야 합니다:
  - **최신순**: 생성 시간 기준 내림차순 (최근 항목이 위로)
  - **오래된순**: 생성 시간 기준 오름차순 (오래된 항목이 위로)
- 현재 활성화된 정렬 기준이 시각적으로 구분되어야 합니다
- 정렬은 할일 목록과 완료 목록 모두에 적용됩니다

## 컴포넌트 분리 가이드

최소 아래 컴포넌트로 분리해야 합니다. 파일은 `src/components/` 폴더에 만듭니다.

| 컴포넌트 | 역할 |
|---|---|
| `TodoForm` | 할일 입력 폼 |
| `TodoItem` | 할일 항목 하나 (완료/삭제/수정 포함) |
| `TodoList` | 할일 목록 (여러 TodoItem) |
| `SortButtons` | 정렬 버튼 |

- 상태(`todos`, `sortOrder`)는 `App.jsx`에서 관리합니다
- 자식 컴포넌트에 함수를 props로 전달하여 상태를 변경합니다
- 수정 중인지 여부(`isEditing`)와 수정 중 입력값은 `TodoItem` 내부에서 관리해도 됩니다

## 스타일링

- CSS Module을 사용합니다
- `App.module.css`에 `.container`와 `.completed` 클래스가 제공되어 있습니다
- 각 컴포넌트별 CSS Module을 만들어도 되고, 하나로 사용해도 됩니다
- 추가 스타일링은 자유롭게 해도 되고, 안 해도 됩니다

## 힌트

- 수정 모드 관리: `TodoItem` 안에서 `const [isEditing, setIsEditing] = useState(false)` 사용
- 정렬: `[...todos].sort((a, b) => new Date(b.createdAt) - new Date(a.createdAt))` (최신순)
- 정렬된 배열을 만들 때 원본 배열을 변경하지 않도록 `[...todos]`로 복사 후 정렬하세요

## 파일 구조 예시

```
src/
├── App.jsx
├── App.css
├── App.module.css
└── components/
    ├── TodoForm.jsx
    ├── TodoItem.jsx
    ├── TodoList.jsx
    └── SortButtons.jsx
```

## 완성 예시 구조

```
┌─────────────────────────────────┐
│          Todo List              │
│                                 │
│  [할일 입력...        ] [추가]  │
│                                 │
│  [최신순]  [오래된순]           │
│                                 │
│  ── 할일 목록 ──                │
│                                 │
│  리액트 공부하기                │
│  2026. 3. 18. 오후 1:00        │
│  [완료] [수정] [삭제]           │
│                                 │
│  (수정 모드일 때)               │
│  [리액트 공부하기    ]          │
│  [저장] [삭제]                  │
│                                 │
│  ── 완료 목록 ──                │
│                                 │
│  장보기                         │
│  2026. 3. 18. 오후 12:30       │
│  [취소] [수정] [삭제]           │
└─────────────────────────────────┘
```
